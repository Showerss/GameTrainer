# Minesweeper (numeric 8x8) profile -- Milestone M5, Brick 6.
# Ground: LibreMines v2.3.0, Easy difficulty (8x8, 10 mines, 54 safe cells).

ground: minesweeper
perception: numeric
reward: minesweeper

total_timesteps: 20000

# PPO hyperparameters -- sensible defaults matching M1/M2/M4
learning_rate: 0.0003
n_steps: 2048
batch_size: 64
n_epochs: 10
gamma: 0.99
gae_lambda: 0.95
clip_range: 0.2
ent_coef: 0.0

# Reward numbers -- MinesweeperRewardCalculator (Milestone M5, Brick 4)
safe_reveal_reward: 1.0
mine_penalty: -10.0
win_reward: 10.0

# Margin over random baseline
margin_over_baseline: 0.0
